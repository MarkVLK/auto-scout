Legacy runtime surfaces that are no longer part of the supported v1 path live here.

Quarantined in the clean-slate reset:
- browser dashboard assets
- web service unit
- voice command entrypoint

The active supported interfaces are now:
- `auto-scout` CLI
- `config/site.yaml`
- `config/missions/smoke_loop.yaml`
- Scout and companion runtime agents
