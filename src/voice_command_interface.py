#!/usr/bin/env python3
"""Deprecated voice entrypoint kept only as a clear quarantine stub."""


def main():
    raise SystemExit(
        "The voice command interface is retired in the clean-slate reset. "
        "Use the `auto-scout` CLI instead, or inspect legacy/voice_command_interface_legacy.py."
    )


if __name__ == "__main__":
    main()
