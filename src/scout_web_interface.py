#!/usr/bin/env python3
"""Deprecated dashboard entrypoint kept only as a clear quarantine stub."""


def main():
    raise SystemExit(
        "The Scout web dashboard is retired in the clean-slate reset. "
        "Use the `auto-scout` CLI instead, or inspect legacy/scout_web_interface_legacy.py."
    )


if __name__ == "__main__":
    main()
